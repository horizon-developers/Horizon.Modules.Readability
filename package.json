{
  "name": "bluebird.modules.readability",
  "private": true,
  "scripts": {
    "build": "esbuild readability.js --minify --bundle --outdir=dist"
  },
  "dependencies": {
    "esbuild": "^0.27.0"
  }
}
